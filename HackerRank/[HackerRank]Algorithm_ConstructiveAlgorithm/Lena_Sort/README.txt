This code use recursion! (Which is bad.)
You have to modify java stack size, otherwise it will stack overflow with large input.
This code will pass HackerRank tests.

Help me if you can provide a non-recursive implementation.